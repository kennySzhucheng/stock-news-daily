# 开发日志

按模块推进，每完成一个模块更新一条记录。

---

## 2026-09-24 · 修 observe.py 时段推断，artifact 带时段 ✅ 完成

**问题**（读代码时发现，详见下一条 9-20~09-24 条目第四节）：`observe.py` 按运行
**触发时刻**是否过 12:00 推时段，而延迟的盘前 cron 要到 12:4x 才跑，会被判成 `pm`。
后果是印错产出文件名；同一天两条都记成 `pm`、后者覆盖前者，令「盘前/盘后两份均已
保留」的汇总判定**永远不打印**（验收项等于悄悄失效）。更隐蔽的是：被去重 `skipped`
的运行与真正产出的运行在输出里长得一模一样，分不出哪次真出了报。

**根因：** 运行对象（`GET /actions/runs`）里**既没有 `inputs` 也没有 `schedule`
字段**——这一条是实测确认的（两者均为 `null`）。所以外部工具单看运行列表，根本
无从知道本次该出哪个时段，靠触发时刻猜在延迟场景下必然出错。

**改动：**

1. **`.github/workflows/daily.yml`** —— 产物名带上时段：
   `daily-data-<run_number>` → `daily-data-<run_number>-<slot>`。
   去重跳过的运行本来就走不到上传步骤（`if` 已拦），所以「有没有产物」同时就是
   「本次有没有真的产出」的判据——一个字段解决两个问题，不必再加步骤。
2. **`modules/m8_e2e/observe.py`** —— 改为读产物名判时段与产出：
   - 运行成功 + 有 `daily-data-*-<slot>` 产物 → 真正出报，时段已知
   - 运行成功 + 无产物 → 被去重跳过，表格里明写「无产出」
   - 运行失败 → 标失败；产物超过 7 天保留期 → 标「无法判定」而不是瞎猜
   - 兼容 9-24 之前不带时段的旧产物名，标成「有产出（时段未知）」，不误判为跳过
   - 顺带修：`GITHUB_TOKEN` 支持（docstring 里一直写着，代码从没实现过），
     且只发给 api.github.com —— `check_url` 访问 github.io 时不带令牌；
     删掉已无用的 `slot_of`
   - 顺带修：线上文件检查的日期改为取自运行记录本身，不再依赖 `by_date`。
     否则产物名是旧命名（时段未知）时 `by_date` 为空，**整段检查一行都不打印**
     —— 这是本次改动的中途回归，已修
   - 小结新增「真正出报 N 次 / 被去重跳过 M 次」。此前只看 `conclusion`，
     而被跳过的运行结论同样是 `success`，会把产出次数算高

### 验收

- [x] YAML 解析通过，15 个步骤完整，`slot` / `force` 两个输入就位，产物名含时段
- [x] `observe.py` 语法与 `py_compile` 通过，`slot_of` 无残留引用
- [x] 本机实跑（`--limit 8`）：产出运行耗时 470~660s，被去重挡下的只要 **10~12s**，
      与逐条核对「M1 新闻收集」步骤结论的结果**完全一致**；线上 8 份文件全部 OK
- [ ] 新产物名要等 workflow 推送后、下次运行才生效。生效前的历史运行会显示
      「旧版产物名，时段未知」，属预期过渡状态

---

## 2026-09-20 ~ 09-24 · 连续 5 天云端观察：去重稳住了，准点仍没解决 ⏳ 部分完成

**起因：** 9-19 修完去重后需要连续观察几天，确认修复没有回归、并核对准点问题到底改善了多少。

**数据来源：** Actions 运行记录（逐个查 M1 步骤是 success 还是 skipped）、gh-pages 部署
提交历史、本机触发日志 `~/.stock-news-daily/trigger-{am,pm}.log`，三方交叉核对。

### 一、送达时间线（北京时间；时刻取 gh-pages 部署提交，微信推送在其中约 1 秒前）

| 日期 | 盘前送达 | 计划 08:10 | 延迟 | 由谁产出 | 盘后送达 | 计划 15:40 | 延迟 | 由谁产出 |
|---|---|---|---|---|---|---|---|---|
| 09-20 | 10:18 | | +2h08m | 本机任务 10:11 | 20:46 | | **+5h06m** | 延迟 cron |
| 09-21 | 13:00 | | **+4h50m** | 延迟 cron | 16:09 | | +0h29m | 本机任务 |
| 09-22 | 12:54 | | **+4h44m** | 延迟 cron | 18:24 | | +2h44m | 本机任务 |
| 09-23 | 12:49 | | **+4h39m** | 延迟 cron | 15:47 | | **+0h07m** ✅ | 本机任务 15:40:02 |
| 09-24 | 12:04 | | +3h54m | 本机任务 11:53 | 17:58 | | +2h18m | 本机任务 |

**5 天 10 次里只有 9-23 盘后一次准点。**

- **盘前几乎必然晚 4 小时左右**：08:10 笔记本一定在睡眠，本机任务跑不了；
  最后是延迟到 12:4x 的 cron 顶上的（9-21 / 9-22 / 9-23 三次都是这样）。
  9-24 例外——机器 11:53 恰好醒着，本机任务抢在延迟 cron（12:39）前面出了报。
- **盘后看运气**：机器碰巧醒着就准（9-23 抢在 15:40:02），否则拖到延迟 cron 的 20:4x。

### 二、去重验证通过（连续 5 天，零重复）

逐日核对「M1 新闻收集」步骤的结论，每天**恰好 2 次运行真正产出**（盘前、盘后各一），
其余运行一律 `skipped`，且**没有任何一天出现重复推送**：

| 日期 | 产出运行 | 被去重挡住的运行 |
|---|---|---|
| 09-20 | dispatch 10:11（盘前）、schedule 20:35（盘后） | schedule 12:48 |
| 09-21 | schedule 12:48（盘前）、dispatch 16:00（盘后） | schedule 22:30 |
| 09-22 | schedule 12:46（盘前）、dispatch 18:14（盘后） | schedule 20:48 |
| 09-23 | schedule 12:39（盘前）、dispatch 15:40（盘后） | schedule 20:58 |
| 09-24 | dispatch 11:53（盘前）、dispatch 17:48（盘后） | schedule 12:39 |

9-19 那种「早上收到两条盘前推送、后一条把带标注的覆盖掉」再没出现过。
gh-pages 每天也恰好 2 次部署，9-17~09-24 每份 `-am/-pm` 齐全，一份不缺。

### 三、准点问题：根因未变，且不靠本机已无解

本机任务靠不住的原因这次又复核了一遍（`Get-ScheduledTask` + `powercfg` 实测）：

- `WakeToRun = False` —— 任务**没有**「唤醒计算机执行」权限
- 电源 `RTCWAKE`：交流 = **仅重要唤醒定时器**，电池 = **禁用**
- `StartWhenAvailable = True` —— 所以只能「等机器碰巧醒过来」才补跑

也就是本机任务永远只能在「机器恰好醒着」时准点。9-19 已定的方案（改用
cron-job.org 云端调度器）**至今未实施**，这也是准点迟迟没改善的唯一原因。

**证据：所有 `workflow_dispatch` 都是本机任务发的，没有一条来自云端调度器。**
本机日志的时间戳与运行创建时间逐秒吻合（如 9-24 `11:53:20` → run `11:53:26`）。

### 四、新发现的缺陷：`observe.py` 的时段推断是错的

连续观察时该用 `modules/m8_e2e/observe.py`，但读代码发现它的时段判定站不住：

- `observe.py:134` 用 `slot_of(created)` —— 按**运行触发时刻**是否过 12:00 推时段。
  这个前提在延迟场景下**不成立**：9-21 盘前那份是 12:48 才跑的 cron 产出的，
  `slot_of` 判成 `pm`，于是把产物报成 `2026-09-21-pm.html`（错）。
- 连带影响到 `by_date`（:136）：同一天两个 run 都被记成 `pm`，后者覆盖前者，
  导致 `:157` 的 `both_ran` 判定永远为 False —— **「盘前/盘后两份均已保留」这行
  核对永远不会打印**，等于该验收项悄悄失效。
- 更隐蔽的是：被去重 `skipped` 的运行与真正产出的运行**在输出里长得一模一样**，
  光看这张表分不出哪次真出了报（上面第二节是我逐个查 M1 步骤结论才确认的）。

线上文件的存在性检查（:143-161）本身是对的，受影响的是「产出文件」列与那份汇总判定。

### 待办

- [ ] cron-job.org 两个 job（用户侧操作，PAT 已备好）
- [x] 修 `observe.py` 的时段推断与「跳过运行」标注 —— 已于同日修复，见上一条
- [ ] 用户试用反馈（M8 验收最后一项）

---

## 2026-09-19 · 去重覆盖 workflow_dispatch（修掉本机任务与 cron 撞车导致的重复推送） ✅ 完成

**当天实况（时间线已按日志逐条核对）：**

| 时刻 | 事件 |
|---|---|
| 08:10 | 笔记本在**睡眠**，本机盘前任务未执行 |
| 12:31:35 | 延迟的盘前 cron 先跑完（延迟 4h21m）→ 推送**带标注**的盘前日报 pushid=57154881 |
| 12:33:09 | 笔记本唤醒，本机任务被 `StartWhenAvailable` 顺延补跑 |
| 12:45 | 该任务**又整条重跑了 9 分钟**，推送**第二条**盘前 pushid=57155656（无标注），并**覆盖**了线上前一条日报 |
| 15:42 | 再次唤醒，盘后任务补跑（迟 2 分钟）→ 15:48 送达 ✅ |
| 20:01:44 | 延迟的盘后 cron → **被去重正确挡住**，M1→M6 全 skipped ✅ |

所以用户当天收到**两条盘前推送**，且后一条把带延迟标注的那份覆盖了。

**根因：`workflow_dispatch` 绕过了去重。** 去重步骤此前只在 `event_name == 'schedule'`
时执行 —— 而本机计划任务走的正是 `workflow_dispatch`。

**改动：**

1. 去重步骤的条件改为
   `github.event_name != 'workflow_dispatch' || github.event.inputs.force != 'true'`
   —— 除「显式要求强制重跑」外，两种触发源一律去重。
2. workflow 新增 `force` 输入；`tools/trigger_workflow.py --force` 会传 `force=true`。

触发源补跑时前一次运行可能仍在进行（并发组会把它排进队列），等它真正开始跑时
gh-pages 上已有产出，故不会误判为「未出报」。

### 验收

- [x] YAML 解析通过，`workflow_dispatch` 两个输入 `slot` / `force` 就位，15 个步骤完整
- [x] 去重条件真值表：schedule → 执行；dispatch+force 空/false → 执行；dispatch+force=true → 跳过
- [x] **真机实测（9-19 21:18，run 35445377628）**：用 fine-grained PAT 发
      `workflow_dispatch`（slot=pm）→ **HTTP 204**；该运行在「检查本时段是否已出报」
      判定 `今日 pm 报告已存在（reports/2026-09-19-pm.html），跳过本次以避免重复推送`，
      M1→部署全部 skipped，**无推送、无覆盖** —— 证明本次修复对 dispatch 生效
      （修复前这次运行会整条重跑并发第二条微信，即当天早上发生的事）
- [ ] 云端调度器配好后，观察一次真实按点触发

### 仍未解决

**笔记本睡眠导致本机任务无法在 08:10 准点执行。** 实测本机电源设置：
睡眠超时 15 分钟（交流）/ 10 分钟（电池），唤醒定时器交流=仅重要、**电池=禁用**。
所以上面那两行「12:33 唤醒」「15:42 唤醒」都是**碰巧机器被唤醒**（用户合盖/开盖），
不是任务把自己唤醒的。本机任务因此只能在「机器恰好醒着」时准点。

---

## 2026-09-18 · 启用本机计划任务，让报告真正按点送达 ✅ 完成

**起因：** 9-18 盘后又是 20:29 才到（cron 20:20:40 才投递，延迟 4h40m）。

**根因：让报告准时的那个部件，从来没有被启用过。**

GitHub 的 schedule 延迟是平台行为，改代码改不好。上一条的去重与延迟标注只保证
「不重复推送」和「读者知道这是延迟版」——**它们不产生准点**。准点只能靠外部按点
触发，而两条外部路径当晚一条都没通：云端 cron-job.org 需要先建 PAT（未建），
本机计划任务当时只作为备选、未注册。于是唯一的触发源仍是延迟的 cron。

**教训：** 当时把云端方案标为「推荐」、把本机方案贬为「依赖电脑开机」，但云端
恰恰需要用户先建 PAT 与第三方账号（卡在用户侧），而本机方案是零额外配置的
——令牌本来就在本机。**能在本机闭环的方案，不该被推给用户去搭。**

### 本次改动

1. **`trigger_workflow.py` 默认复用本机 `gh auth token`**
   原先要求先 `setx GITHUB_TOKEN`，对本机任务纯属多余门槛：令牌本就由 gh 保管，
   直接借用即可，不必落到环境变量、也不必为这条路新建 PAT。
   令牌按 `--token` > `GITHUB_TOKEN`/`GH_PAT` > `gh auth token` 依次取。

2. **新增 `--log-file`**：计划任务没有控制台可看，输出追加写入文件（带时间戳分节），
   否则触发失败将无从排查。

3. **注册两个计划任务**（用户授权后执行）：`股市情报-盘前` 08:10 / `股市情报-盘后` 15:40，
   日志在 `~/.stock-news-daily/trigger-{am,pm}.log`。

4. **修掉一个会静默漏触发的默认值**：`Register-ScheduledTask` 默认
   `DisallowStartIfOnBatteries=True`，**笔记本用电池时任务根本不执行、且不报错**。
   本机确认是笔记本（有电池），已改为 `-AllowStartIfOnBatteries
   -DontStopIfGoingOnBatteries`。`--print-task-cmd` 也改为输出 PowerShell 写法
   （`schtasks /Create` 没有这个开关），避免以后重新注册时把坑带回来。

### 验收

- [x] 两个任务 `State=Ready`，`股市情报-盘前 / 股市情报-盘后` 下次运行 2026-09-19 08:10 / 15:40
- [x] 执行内容与触发时间正确（`StartBoundary 08:10:00+08:00`）
- [x] 电池限制已关闭（`DisallowStartIfOnBatteries=False`）
- [x] **链路实测**：手动 `Run` 一次盘前任务 → 日志写入成功
      （UTF-8 无 BOM，记事本可读）→ 正确判定 `[skip] 2026-09-18 的盘前报告已存在`
      → 说明「任务 → python → gh 令牌 → gh-pages 查重」四段全通
- [ ] 等 2026-09-19 08:10 / 15:40 的**真实自动触发**，确认按点送达

### 已知限制

任务以「交互式登录」身份运行，要求电脑**开着且已登录**；注销状态下不跑。
该次会退回延迟的 cron 兜底（此时报告带延迟标注）。若只是休眠/关机错过时点，
`StartWhenAvailable` 会在恢复后尽快补跑一次。

**另一处未闭合：** 手动/计划任务触发走 `workflow_dispatch` 且带 `inputs.slot`，
`PLAN` 为空 → **不标注延迟**。所以「开关机错过时点后被 StartWhenAvailable 补跑」
产生的那份报告，是迟到但不带标注的。与 cron 兜底路径的标注行为不一致，暂记在此。

---

## 2026-09-18 · 去重改为按时段判定（修掉 9-17 症状的直接原因） ✅ 完成

**起因：** 复查上一条改动时发现两件事——**改动一行都没上线**（远程 `main` 仍停在
`e796af62`，延迟标注 / 外部触发 / 去重全在本地未提交），而且**去重逻辑本身就是
9-17 症状的直接原因**。

### 关键发现：旧去重不看时段

`tools/trigger_workflow.py` 的 `recent_runs()` 与 workflow 的去重步骤都按
「最近 N 小时是否跑过」判断，与时段无关。9-17 的真实链路：

| 步骤 | 结果 |
|---|---|
| 1. 盘前 cron 延迟到 12:46 才跑 | 平台延迟，无解 |
| 2. 15:40 外部触发盘后 | 查到 2h54m 前刚跑过 → **skip** |
| 3. 盘后只能退回等延迟的 cron | 20:45 跑 → 20:53 推送 |

**外部调度器本来能救盘后，却被自己的去重挡住了** —— 延迟的盘前运行不仅晚，
还顺手把盘后的正常触发挤掉。另：旧逻辑的 6 小时窗口对延迟更久的 cron 失效，
会重复出报 + 重复推送。

### 本次改动

1. **workflow 去重改为「今天的这个时段是否已出报」**（`.github/workflows/daily.yml`）
   - 判定用 gh-pages 上的产出文件（`恢复历史日报` 已解到 `reports/`），
     即 `reports/YYYY-MM-DD-<slot>.html` 是否存在
   - 精确到「日期 + 时段」，且不再需要 `curl` + `jq` + `actions: read`
     （`permissions` 退回只留 `contents: write`）
   - `恢复历史日报` 新增 `id: restore` 与 `ok=true/false`：gh-pages 拉取失败时
     无从判断，显式放行（宁可重复一次，也不能让去重挡住出报）
   - 附带好处：早先那次运行若在 M5 之前失败，文件不存在 → 自动重试

2. **`tools/trigger_workflow.py` 同步改法**
   - `recent_runs(hours)` → `report_exists(date, slot)`，查 gh-pages 上
     `{date}-{slot}.html`（`/repos/{repo}/contents/...?ref=gh-pages`）
   - 返回 `True` 已出报 / `False` 未出报 / `None` 查询失败（按未出报放行）
   - 去掉已无用的 `--within-hours`

3. **推送方式**：本机 `github.com:443` 走代理不通，用 `tools/push_via_api.py`
   经 `api.github.com` 推送（该工具只推**已提交的 HEAD**，故须先 commit）

### 验收

- [x] 去重按时段判定（对真实 gh-pages 实测）：
      `09-16 am=False / pm=True`（那天只有不带后缀的旧文件与 `-pm.html`）、
      `09-17 am/pm=True`、`09-18 am/pm=False`
      → 证明判定精确到时段，而非「那天有文件就算」
- [x] **9-17 回放**：15:40 时 `09-17-pm` 为 `False` → 盘后会被正常触发（旧逻辑会 skip）
- [x] 延迟标注四场景：276 分钟标注 / 305 分钟标注 / 5 分钟不标注 / 无环境变量不标注；
      M5 与 M6 的 `delay_context()` 输出一致
- [x] 日报实际渲染：标题为「股市情报日报 · 2026-09-17 盘前（延迟4h36m）」，
      页顶 `<div class="delay-banner">` 出现（276 分钟出现 / 5 分钟不出现）
- [x] workflow YAML 解析通过，15 个步骤与 `if` 条件完整
- [x] **云端端到端实测（9-18）**：
  - 外部触发：`workflow_dispatch` 返回 HTTP 204，运行立即创建，8 分 26 秒跑完全链路，
    产出 `2026-09-18-am.html`，微信推送 pushid=56975454
  - **去重（关键）**：延迟的盘前 cron 随后触发（run 35307560447），
    日志 `今日 am 报告已存在（reports/2026-09-18-am.html），跳过本次以避免重复推送`，
    M1→Pages 全部 skipped，**无重复推送、无覆盖**
  - **延迟计算（验证 tzdata 依赖）**：同一运行日志 `计划时点: 08:10　延迟: 272 分钟`，
    与实际执行时刻 12:42:12 相减吻合。此前担心的「runner 上 `TZ=Asia/Shanghai` 不生效
    会导致延迟算成负数、静默丢失标注」**已排除**，无需改成不依赖 tzdata 的算法
- [ ] 外部调度器（cron-job.org）配好后，观察一次真正的按点触发

### 待办：外部调度器

本机计划任务依赖电脑在 08:10/15:40 开机，改用 **cron-job.org** 按点 POST
`workflow_dispatch`（时区选 Asia/Shanghai）。GitHub 自带 cron **保留作兜底** ——
这正是去重必须先修的原因：外部调度器失效时 cron 仍能出报，且两者撞车不会重复推送。
外部触发走 `workflow_dispatch` 带 `inputs.slot`，`PLAN` 为空 → **不标注延迟**
（准点的那次不该被标注）；只有延迟的 cron 才标注，两条路径互补。

**令牌安全提醒：** `gh auth token` 的 scope 是 `read:org, repo, workflow`，
其中 `repo` 意味着能读写该账号下的**全部**仓库（含私有）。把它交给第三方调度器
风险过大，应新建 fine-grained PAT，只授权 `stock-news-daily` 这一个**公开**仓库的
**Actions: Read and write**。

---

## 2026-09-18 · 云端定时延迟核对 + 延迟标注 / 外部触发 ✅ 完成

**起因：** 用户反馈 9-17 盘前推送 12:55 才收到、盘后 20:53 才收到（计划 08:10 / 15:40）。

**核对结论：不是推送慢，是 GitHub Actions 的 schedule 事件被延迟投递。**

| 计划时点 | 实际开始（北京时间） | 延迟 |
|---|---|---|
| 09-17 08:10 盘前 | 12:46:28 | 4h36m |
| 09-17 15:40 盘后 | 20:45:36 | 5h05m |
| 09-18 08:10 盘前 | 至 09:58 仍未触发 | >1h48m |

- 每次运行自身耗时 8~9 分钟，M6 在流程末尾，故推送时间 = 实际开始 + 8 分钟，
  与用户收到的 12:55 / 20:53 精确吻合
- `run_started_at` 与 `created_at` 相同（排队 0 秒）→ 不是队列堆积，
  是调度器压根没按时投递
- GitHub Status 同期无 Actions 相关故障，排除平台事故

**副作用（比晚点更值得修）：时段标签与内容错配。**
线上 `2026-09-17-am.html` 标题写「盘前」，正文最新新闻却是 12:19——12:46 才开跑，
抓的是那一刻前 24 小时的新闻，里面已经混进当天上午的盘中消息。根因是时段由
cron 字符串判定（`10 0 * * *` → am），与实际运行时间完全脱钩。

### 本次改动

1. **延迟标注（workflow → M5 / M6）**
   - workflow 在「判定运行时段」里算出计划时点与延迟分钟数，经
     `REPORT_PLAN_TIME` / `REPORT_DELAY_MIN` 两个环境变量传给 M5、M6
   - 延迟 ≥15 分钟才标注——cron 数分钟抖动属正常，不必打扰
   - 日报：标题加「（延迟4h36m）」，页顶加橙色提示条，写明实际运行时点并说明
     "内容采集自实际运行时刻前 24 小时，并非严格意义的盘前/盘后快照"
   - 推送：标题改用短日期腾出字符（`📊 股市情报日报 09-17 盘前·延迟4h36m`，
     25 字符，在 Server酱 32 字上限内），正文首行加引用块说明
   - 本地运行与手动触发拿不到这两个变量 → 不标注

2. **`tools/trigger_workflow.py`：从外部按点触发，绕开 cron 队列**
   - 触发前查最近 6 小时内是否已有运行，有则跳过（`--force` 可忽略）
   - `--dry-run` / `--wait`（等运行结束）/ `--print-task-cmd`（打印 schtasks 注册命令）
   - 需一枚 fine-grained PAT（Actions: read and write），`setx GITHUB_TOKEN <PAT>`
   - docstring 里附了等效 curl，可直接挂 cron-job.org 这类云端调度器

3. **workflow 去重：schedule 触发时先查 6 小时内是否已有「股市情报日报」运行，
   有则跳过全部主步骤。** 避免本机按点触发后，延迟数小时的 cron 又重跑一遍
   （重复推送 + 覆盖日报 + API 白跑）。本机没开机时查不到运行，cron 照常兜底。

### 验收

- [x] 三种场景渲染验证：延迟 4h36m 标注 / 抖动 5m 不标注 / 本地运行不标注，全部通过
- [x] 推送标题 25 字符，在 Server酱 32 字上限内
- [x] `trigger_workflow.py --print-task-cmd` 输出可直接执行的 schtasks 命令
- [ ] 待推送到 GitHub 后，在云端实际观察一次延迟标注效果
- [ ] 待用户提供 PAT 后注册本机计划任务

**附带澄清：** 运行列表里的 `event=dynamic` 是 GitHub Pages 的
`pages build and deployment` 工作流（head_branch=gh-pages），不是重复运行。
`observe.py` 未按工作流名过滤，会把它计入运行次数。

---

## 2026-09-17 · cron 定时任务激活与推送验证 ✅ 完成

**问题发现：** GitHub Actions cron 定时任务从未被触发过（所有历史运行均为 workflow_dispatch 手动触发或 dynamic 事件），导致今天 08:10 盘前日报未按时推送。

**根本原因：** GitHub Actions 需要首次手动激活才能启用 cron 调度机制。

**修复操作：**
- 手动触发 workflow（run 35169710420），激活 cron 定时调度
- 验证所有 GitHub Secrets 配置正确：ZAI_API_KEY、DEEPSEEK_API_KEY、SERVERCHAN_SENDKEY
- M6 微信推送成功：pushid=56723358，标题「📊 股市情报日报 2026-09-17 · 盘前」

**预期效果：**
- 今天下午 15:40 的 cron 任务应该能够自动触发
- 后续每天 08:10 / 15:40 的定时运行应正常工作

**状态：** 等待今天 15:40 验证 cron 是否自动触发

---

## 2026-09-16 · 网页版上线 + 推送链路加固 ✅ 完成

**线上地址（国内直连，手机无需梯子）：**
- 日报索引：https://kennyszhucheng.github.io/stock-news-daily/
- 当日日报：`/2026-09-16-pm.html`（盘后）
- **网页版：https://kennyszhucheng.github.io/stock-news-daily/web/**
- 微信推送：本次 pushid=56655308

**云端验证运行（run 35112231681）：18 个步骤全部成功，耗时 8 分 26 秒。**
M1 收集 1118 条（6/6 源可用）→ M2 结构化 150 条 → M3 深度分析 →
M4 行情 21 只 → M5 日报 → M9 网页版导出 → M6 微信推送 → Pages 部署。

**定时任务已确认生效：** 默认分支 main 上的 workflow 带两个 cron
（`10 0 * * *` 盘前 / `40 7 * * *` 盘后），三个 Secrets（ZAI / DEEPSEEK /
SERVERCHAN）齐备。明天的两次运行会自动产出 `-am` 与 `-pm` 两份日报，
并把网页版一并部署。

### 本次加固

1. **网页版导出改为非阻塞并移到推送之后。**
   原顺序是 M5 → M9 → M6 → 部署，M9 是普通步骤，一旦出错会连带跳过
   微信推送和 Pages 部署——一个附加产出不该有能力掐断主链路。
   现为 M5 → M6 推送 → M9 导出（`continue-on-error`）→ 部署。

2. **新增 `tools/push_via_api.py`。**
   国内 github.com 的 443 时常不通（`git push` 报 Connection was reset /
   Could not connect），但 api.github.com 可直连。该工具用 Git Data API
   复刻一次等效 push（blob → tree → commit → ref），支持 `--dry-run`。

   开发过程中踩到两个坑，都已在代码里注明：
   - **不能读工作区文件取 blob 内容。** `core.autocrlf=true` 时工作区是
     CRLF、git 里存的是 LF，读工作区会把 CRLF 提到远程，导致远程 blob
     与本地 HEAD 对不上，之后 `git fetch/reset/diff` 全会错乱。
     改用 `git cat-file blob HEAD:<path>` 取对象内容。
   - **推送后必须自检。** 工具现在会重新读一次远程提交、比对文件树，
     不一致就报错退出。上面那个 CRLF 问题如果有自检，第一次就能发现。
   - 附带：经本工具推过的分支，远程 SHA 本地并不存在，git 的祖先判断
     用不上，所以同步状态按**文件树**判断而非提交 SHA。
   - api.github.com 会间歇性 RemoteDisconnected，API 调用已带退避重试；
     实测首次推送就在最后更新 ref 那步断过。

3. **索引页：无时段后缀的旧命名日报标为「早期」。**
   gh-pages 里还留着加时段命名之前产出的 `2026-09-16.html`，
   它没有 am/pm 后缀，在索引页里会以默认标签「日报」与「盘后」并列，
   读起来像是两种不同的东西。此后不会再产生这类文件。

### 本地与远程的同步状态

本地 `main` 与远程 `main` **文件内容完全一致**（文件树相同），
仅提交 SHA 不同——这是经 API 推送的必然结果。
梯子恢复后执行 `git fetch && git reset --hard origin/main` 即可对齐。

### 验收

- [x] 线上网页版可打开、数据完整（8/8 个数据文件解析正常，浏览器实测渲染无误）
- [x] 线上日报版块顺序正确（市场分析在个股行情与分板块新闻之前）
- [x] 微信推送成功（pushid=56655308）
- [x] 定时触发器随推送生效，明天的两次运行无需人工干预
- [ ] 明天观察 08:10 / 15:40 两次定时运行是否如期触发（cron 可能被 GitHub 延迟）

---

## 2026-09-16 · M9 网页版 + 四项改进 ✅ 完成

按用户反馈做的四项改进，外加过程中发现并修复的 5 个真实缺陷。

### 改进 1：同日两次运行不再互相覆盖（方案 B）

原问题：08:10 与 15:40（北京时间）换算成 UTC 是 00:10 与 07:40，同属一个 UTC 日，
M5 按运行日期命名 → 下午的运行覆盖上午，盘前观点不留存。

- 文件名加时段后缀：`reports/YYYY-MM-DD-am.html`（盘前）/ `-pm.html`（盘后），**两份都保留**
- 时段按北京时间 12:00 划分；M5/M6/observe 共用同一判定逻辑
- 判定优先级：命令行 `--slot` > 环境变量 `REPORT_SLOT` > 自动判定
- 新增 `reports/latest.html`：固定入口，始终指向最近一次运行
- 索引页改为按日期分组，每天列出盘前/盘后两条
- workflow 用 cron 字符串判定时段（比按时间推算可靠），手动触发可指定
- `observe.py` 相应改造：分别核对 am/pm 两份是否都已上线

### 改进 2：市场分析提到最前

- **M5 日报**：版块顺序由「摘要 → 情绪概览 → 个股行情 → 分板块新闻 → 完整分析」
  改为「摘要 → **市场分析** → 个股行情 → 分板块新闻」。
  原第 2 版块「情绪概览」与末尾「完整分析」本渲染的是同一份 M3 文本（重复两遍），
  合并为一份。新增顶部版块跳转导航。
- **M6 推送**：原来正文只有一句情绪结论，现改为先给完整市场分析
  （情绪结论 + 最多 3 个关注板块的逻辑链），3 条重点新闻退居其后。
  推送里没有新闻编号表，故板块逻辑链中的 `[12]` 类引用会被剥掉，避免读者无从对照。
- 标题也带上时段：`📊 股市情报日报 2026-09-17 · 盘前`

### 改进 3：M9 交互式网页版（本地服务 + 静态导出）

推送版是"读一遍就走"的长文；M9 面向"我想自己找"的场景，功能多于推送版。

| | M5 日报 | M9 网页版 |
|---|---|---|
| 检索 | 无 | 全文搜索 + 六维筛选 |
| 引用 `[12]` | 只是文本 | 可点击，直接跳到出处新闻 |
| 原始新闻 | 不展示 | 可查看被 M2 过滤掉的 1000+ 条 |
| 聚合 | 表格与标签 | 板块热度、多空分布、涨跌榜 |
| AI 追问 | 无 | 可就今日新闻提问，答案带出处与置信度 |

- 目录：`modules/m9_web/`，`aggregate.py`（数据聚合）/ `ask.py`（追问）
  / `server.py`（本地服务）/ `export.py`（静态导出）/ `sync.py`（拉取云端产出）
  / `web/`（前端）/ `check.py`（自检）
- **只用 Python 标准库**，无需 pip 安装任何东西
- 服务按数据文件 mtime 自动重载，流水线跑完刷新页面即可，不用重启
- 同一套前端跑两种数据源：`server.py` 时走 `/api/*`，`export.py` 时数据以
  `<script>` 注入 `window.__DATA__`。**用 script 而非 fetch 读 json**——
  浏览器 CORS 禁止 `file://` 页面 fetch，双击打开会报 "Failed to fetch"，
  改用 script 后导出的页面既能挂 Pages 也能直接双击
- 原始新闻 500KB+，改为勾选「包含被过滤的原始新闻」时才懒加载
- **AI 追问**复用 M3 的 DeepSeek 调用与同一套硬约束（禁止编造 / 标置信度 /
  禁止投资指令 / **新闻不足以回答时必须明说**），并要求分析正文里的 `[n]`
  在追问答案里同样可点击
- 线上挂在 Pages 的 `/web/`，随日报一起部署（无后端，追问功能需本地运行）

**本地数据的两种来源（`--source auto|local|export`）：**
- `local`：本地流水线产出 `data/`
- `export`：`sync.py` 从 Pages 拉回来的云端产出 `reports/web/api/`
- `auto`（默认）：**取两者中较新的那一份**。不能简单"有本地就用本地"——
  用户跑完 `sync.py` 之后本地 `data/` 往往还是旧的，那样反而看不到刚同步的内容

**`sync.py`：** GitHub Actions 每天两次跑完就部署，本地不必重跑流水线
（约 6 分钟 + API 费用）也能看到当天最新内容。同步内容为网页版全部资源
（11 个文件，约 680KB）加最近 10 天的日报 HTML；后者拉到本地 `reports/`，
这样本地服务的「历史」页点开就是本地文件，不依赖线上可达。
首次部署前线上还没有 `/web/`，脚本会识别 404 并提示等下一次定时运行。

### 改进 4：每天自动维护

workflow 新增「判定运行时段」与「M9 导出网页版」两步，
每天两次运行都会重出日报与网页版并部署，无需人工干预。

### 过程中发现并修复的 5 个真实缺陷

**缺陷 A（严重）：M4 行情全军覆没，40 只成功 0 只**
- 现象：端到端跑完 `quotes.json` 为空，日报与网页版的行情版块整块缺失
- 三层根因：
  1. 东财 `push2` 主域名限流（`RemoteDisconnected`）→ 实测 **`push2delay.eastmoney.com`
     在限流期间仍可用**，已加为备用域名，每次尝试两个域名都走一遍
  2. `_to_tencent_code` 把美股前缀 106/107 错当成沪/深，美股被拼成 `shJNJ`
     这种不存在的代码，请求必然失败
  3. 熔断器把「该个股不支持」算成「行情源不健康」：开头 7 只美股连续失败就
     触发了 5 次上限，把后面 33 只 A 股一起跳过。
     现在「该市场在备选源无对应代码」单列为 `unsupported_market`，不计入熔断
- 修复后 **34/40 成功**（A股 16 / 港股 2 / 美股等 16）

**缺陷 B（严重）：美股/港股行情价格放大 10 倍**
- 现象：苹果显示 3313.4（实际 331.34）
- 根因：东财 `f43`/`f60` 的缩放系数**随市场而变**——实测沪深 `f59=2`（÷100），
  美股与港股 `f59=3`（÷1000）。代码写死 ÷100
- 修复：请求 `f59` 字段并按其动态换算；`f170` 涨跌幅固定 ×100 不变

**缺陷 C（严重）：腾讯备选源会给美股返回假的 0.00%**
- 现象：休市时段取美股，腾讯返回 `[4]==[3]`、`[32]=0.00`
- 根因：这不是"平盘"，而是根本没有当日数据；我们的定时任务跑在北京时间
  早/下午，正好是美国休市时段
- 修复：涨跌幅改取权威字段 `[32]`（A股收盘后仍为真实值），
  且美股在 `price == prev_close 且 pct == 0` 时判为无数据而非给假值

**缺陷 D（严重）：M2 的 board 字段把公司名当板块**
- 现象：板块页 31 个"板块"里 20 个是公司名（雷柏科技、宁德时代、招商蛇口…），
  板块聚合视图基本失效
- 两层根因：prompt 对 board 几乎没有定义；且 prompt 的 JSON 示例用了占位符
  `"board":["行业或概念名"]`，模型直接照抄，一条数据里出现 30 次
- 修复：① prompt 明确 board 是行业/概念/主题并给出正反例与禁用清单，
  示例改成具体值并注明"只是格式示意，不要照抄"；
  ② 代码层加 `normalize_boards()` 确定性兜底（与个股名全等的一律剔除，
  只做全等比较以免误伤"创投"vs"浦东创投集团"）

**缺陷 E（严重）：三个模块都提取不到 M3 的市场结论**
- 现象：M5 摘要框显示成 `今日市场：## 一、市场情绪概览`，
  M6 推送会说"暂无市场情绪判断"
- 根因：M3 会不定期把整行写成 `**结论：中性偏谨慎**——…`，加粗标记落在行首，
  而 M5/M6/M9 的正则都要求行首必须是「结论」，全部漏掉；
  M5 的兜底逻辑"取第一行正文"又正好抓到标题行
- 修复：三处都改为先剥掉 markdown 加粗再匹配；M5 兜底逻辑增加跳过标题行

### 新增/调整的工具

- `modules/m9_web/check.py`　M9 自检：18 项接口与数据一致性检查
- `modules/m9_web/sync.py`　把云端每天的最新产出拉到本地
- `modules/m8_e2e/e2e_run.py`　新增 M9 步骤与 `--slot` 参数；M5 校验项改为
  核对时段命名、版块顺序（市场分析必须在新闻列表之前）、latest.html
- `modules/m8_e2e/observe.py`　按 am/pm 分别核对线上产出，并检查 `/web/`

### 验收

- [x] 本地端到端跑通（M1→M2→M3→M4→M5→M9），耗时约 6.8 分钟，**全部检查项通过**
- [x] M9 自检 18/18 通过（local 与 export 两种数据源各测一遍）
- [x] 无头浏览器渲染验证：总览/新闻/板块/静态导出（file:// 直开）均正常，无 JS 错误
- [x] AI 追问实测：问"半导体设备相关消息"，正确检索到对应新闻，
  回答区分了"新闻里说了什么"与"据此能推到/推不到什么"
- [x] 同日两份并存：`2026-09-16-am.html` 与 `-pm.html` 同时存在，索引页分别列出
- [x] 板块字段质量：76 个标签全为真实行业/概念，无公司名、无占位符
- [x] M4 行情：39/45 只成功（此前 0/40），美股价格与腾讯源逐只核对一致
- [ ] 云端验证：等下一次定时运行确认 Pages 上的 `/web/` 与两份日报

### 本次实测数据（2026-09-16 盘后）

| 环节 | 结果 |
|---|---|
| M1 新闻收集 | 1025 条，6/6 源成功 |
| M2 新闻筛选 | 150 条结构化，丢弃 552 条，板块清洗 108→104 |
| M3 深度分析 | 4259 字符，禁用词 0 命中，置信度标注 4 处 |
| M4 行情数据 | 39/45 只（A股 17 / 港股 2 / 美股等 20） |
| M5 日报生成 | 125 KB，版块顺序 市场分析→个股行情→分板块新闻 |
| M9 网页版导出 | 687 KB，11 个文件 |

---

## 2026-09-16 · M0 项目骨架与文档（进行中）

**已完成：**
- 确定项目定位：信息助手，不提供买卖指令，决策权归用户
- 确定架构：GitHub Actions 定时 + 六层流水线（收集→筛选→分析→行情→日报→推送）
- 确定技术分工：GLM-4-Flash 做筛选（免费），DeepSeek 做深度分析（低成本）
- 确定数据源策略：多元化（财经+综合门户+科技+官方），多源交叉验证保证准确性
- 创建工作目录与基础文档：README.md、docs/tasks.md、docs/sources.md、docs/keys.md

**数据源验证结果：**
- 新浪财经 7x24：✅ 可用
- 东方财富快讯：✅ 可用（需补 req_trace 参数）
- 东方财富个股行情：✅ 可用
- 财联社：❌ 反爬，放弃

**下一步：** M1 新闻收集模块

---

## 2026-09-16 · M1 新闻收集模块 ✅ 完成

**接入 6 个源（多元化达成）：**
- 快讯类：新浪财经 7×24（399 条）、东方财富 7×24（300 条）
- 科技类：36氪 RSS（30 条）
- 官方综合：新华网时政（300 条）、人民网时政（100 条）
- 政府发布：中国政府网政策库接口（40 条，含文号与摘要）

**单日实测：去重后 1115 条，四类分布 财经/科技/政策/官方。**

**实现要点：**
- 统一输出结构 `{time, source, category, url, text}`，写入 `data/raw_news.json`
- 跨源去重（文本主干归一 + 时间倒序）
- 单源失败不阻塞（status 中记录 error，其余源继续）
- 请求间隔 0.6s，带浏览器 UA

**踩过的坑（供后续模块/维护参考）：**
1. 东财接口 `showTime` 是字符串时间不是毫秒时间戳
2. 36氪 RSS 的 XML 含非法字符/结构，ET 解析失败 → 增加字符清洗 + 正则逐条回退解析
3. `time.localtime` 传毫秒值会 OSError，需 /1000
4. 36氪 pubDate 格式 `2026-09-16 15:58:46  +0800`（双空格 + 无冒号时区），先压缩空白再用 `%z` 解析

**输出文件：** `data/raw_news.json`（291KB / 单日）

**验收：** ✅ 样例抽查字段完整（0 缺失）、输出 826→1115 条（>50 条要求达成）、覆盖 4 类来源

**下一步：** M2 新闻筛选与结构化模块（需 GLM-4-Flash 密钥，已有）

---

## 2026-09-16 · M2 新闻筛选与结构化模块 ✅ 完成

**流程设计（两级漏斗，省钱）：**
```
1115 条原始新闻 → 本地关键字预筛（150 条入 LLM，丢弃 641 条纯社会新闻）
                → GLM-4-Flash 分 8 批结构化
                → 代码层确定性交叉验证
                → 输出 145 条结构化新闻
```

**输出结构：** 每条含 `{time, source, url, text, category, board[], stocks[], sentiment, verified}`
- 分类分布：stock 60 / policy 43 / international 29 / other 9 / industry 4
- 可信度：confirmed 2 / unverified 143（严格，宁缺勿滥）
- 含个股提取 64 条、含板块提取 56 条

**架构决策（重要）：交叉验证从 LLM 移到代码层**
- 首轮实测发现 GLM 批内判断 unreliable：数据中实际跨源同事件仅 1 组（2 条），模型却标了 41 条 confirmed
- 改为代码层确定性比对：正文主干（去标点数字后前16字）一致 + 来源 ≥2 个独立源 → confirmed
- LLM 的 prompt 中删除 verified 字段，专注分类/提取职责

**实现要点：**
- prompt 硬约束：只基于原文、不得编造、board/stocks 只填原文提及的、sentiment 只描述新闻本身倾向不做股价预测
- 兜底策略：LLM 失败批 = 全保留 + neutral 待 M3 处理（宁多勿漏）
- 分类归一化：`finance→stock` 等别名映射到合法五分类
- 本地预筛规则：finance/policy 类全保留，official/tech 类关键字匹配（股|央行|利率|CPI...），纯社会内容直接丢弃

**踩过的坑：**
1. GLM 偶发输出非法分类值（`finance`）→ normalize_cat 别名归一
2. LLM 批量交叉验证不可靠（同批内未见即标 unverified，且高分错标）→ 移交代码层
3. 一次 API 读超时 → glm_chat 带重试（2次）+ 兜底保留，实测第二次全量跑通 0 失败

**验收对照（tasks.md）：**
- [x] 分类准确率人工抽查 ≥80%（抽查 policy/stock/industry/international 四类样例均正确）
- [x] confirmed/unverified 标记正确（确定性算法保证：confirmed 样例真为跨源）
- [x] 单源待核实标记贯穿保留

**输出文件：** `data/structured_news.json`
**下一步：** M3 深度分析模块（DeepSeek 密钥已有）

---

## 2026-09-16 · M8 集成测试与端到端验证（进行中）

**1. 本地端到端全链路跑通 ✅**

| 模块 | 耗时 | 结果 |
|---|---|---|
| M1 新闻收集 | 15.1s | 1023 条，6/6 源成功 |
| M2 新闻筛选 | 333.7s | 130 条结构化，5 类齐全，8 批全部成功 |
| M3 深度分析 | 10.3s | 3333 字符，禁用词 0 命中 |
| M4 行情数据 | ~60s | 19 只（东财限流 → 自动切腾讯源） |
| M5 日报生成 | 0.1s | 69583 字符，四版块齐全 |
| M6 微信推送 | 1.7s | pushid=56637710 |

**2. 过程中发现并修复两个真实缺陷（均为严重级）**

**缺陷 A：Windows 系统代理导致全链路失效**
- 现象：本地端到端卡在 M1 无法推进，所有请求超时
- 根因：urllib 在 Windows 上自动读取系统代理设置。梯子开着但节点不通时，
  所有模块的请求都被送往已死的代理
- 实测（同一请求）：直连 HTTP 200 / 0.2s　vs　走系统代理 SSL handshake 超时
- 修复：各模块新增 `_urlopen()`，优先直连、失败才回退系统代理
- 影响面：**不修则任何开着梯子（哪怕节点已挂）的用户，本地完全无法运行**

**缺陷 B：M4 行情接口限流拖垮整条流水线**
- 现象：M4 超时 600 秒，导致 M5/M6 不执行，端到端中断
- 根因：东财 `push2` 存在按 IP 的临时限流（表现为 `RemoteDisconnected`），
  而重试策略是 5s/10s/15s 指数退避 —— 34 只股票最坏要等 17 分钟，
  违反 tasks.md「接口失败静默跳过该个股」的要求
- 修复：① 重试等待降为 1.5s/3s、单次超时降到 5s；
  ② 东财连续失败 3 次即停用该源，切换备选源；
  ③ 两源合计连续失败 5 次则放弃剩余个股
- **新增备选源**：腾讯行情 `qt.gtimg.cn`（实测 0.3s 稳定可用，无需 Referer，
  GBK 编码，`~` 分隔字段）；同时验证新浪 `hq.sinajs.cn` 可作二级备选
- 修复后 M4 从「超时 10 分钟」降到约 1 分钟

**3. 新增工具**
- `modules/m8_e2e/e2e_run.py`　端到端运行器：分步执行、产出校验、耗时统计、汇总报告
- `modules/m8_e2e/observe.py`　云端观察工具：查 Actions 记录、核对线上日报可达性

**4. 待办（M8 未完成部分）**
- [ ] 连续观察 3 天云端产出（9/17–9/19），运行 `observe.py` 核对
- [ ] 用户试用反馈：日报长度、信息密度是否合适，据此微调

**5. 待用户决策：同日两次运行互相覆盖**
- 08:10 与 15:40（北京时间）换算成 UTC 是 00:10 与 07:40，**同属一个 UTC 日**
- M5 按运行日期命名文件 → 两次都生成 `YYYY-MM-DD.html`，下午的运行覆盖上午
- 结果：线上永远只保留当天最后一次，盘前观点不留存
- 选项：A 保持现状（总是最新）／B 文件名加时段后缀（保留盘前+盘后两份）／C 只保留一次运行

---

## 2026-09-16 · M7 GitHub Actions 工作流 + Pages 部署 ✅ 完成

**线上地址：**
- 代码仓库：https://github.com/kennySzhucheng/stock-news-daily （公开）
- 线上日报：https://kennyszhucheng.github.io/stock-news-daily/

**首次云端运行成功（run 35092383171）：** M1→M6 + Pages 部署全部步骤 ✅，全程约 12 分钟。

**工作流（`.github/workflows/daily.yml`）：**
- 定时：每天北京时间 08:10（盘前）/ 15:40（盘后），cron 为 `10 0 * * *` 与 `40 7 * * *`（UTC）
- 支持 `workflow_dispatch` 手动触发（M8 联调用）
- 并发组锁：避免两次运行重叠互相覆盖
- 恢复历史日报：从 gh-pages 拉回往期报告，保证索引页不只剩当天一份
- 源可用性检查步骤（`if: always()`）：产出 M7 验收第 5 条所需的实测数据
- 部署：peaceiris/actions-gh-pages → gh-pages 分支 → Pages

**密钥配置（GitHub Secrets，3 个）：** `ZAI_API_KEY` / `DEEPSEEK_API_KEY` / `SERVERCHAN_SENDKEY`。代码只从环境变量读取，不硬编码。

**⚠️ M7 验收第 5 条实测结果：海外服务器抓国内源 6/6 全部可用，0 失败。**

| 源 | 本地 | 云端 |
|---|---|---|
| 新浪财经 7×24 | 399 条 | 300 条 |
| 东方财富 7×24 | 300 条 | 300 条 |
| 36氪 | 30 条 | 30 条 |
| 新华网 | 300 条 | 300 条 |
| 人民网 | 100 条 | 100 条 |
| 中国政府网 | 40 条 | 40 条 |

结论：**无需国内中转**。唯一差异是新浪返回条数略少（疑为 IP 相关策略），不影响使用。详细记录见 `docs/sources.md` 第二节。

**首次云端产出：** 结构化新闻 127 条（本地同日 145 条，差异来自新浪返回量）、行情 21 只、日报 72555 字节、微信推送 pushid=56631447。

**踩过的坑（重要，供维护参考）：**
1. **`github.com` 在国内被墙，但 `api.github.com` 可直连** → `gh` 命令能用、`git push` 失败。实测 `api.github.com` 200 / `github.com` 超时 / `ssh.github.com` 不通 / `codeload.github.com` 可达。
   解决：配置 git 走本地代理（`git config http.proxy http://127.0.0.1:7897`，仅本仓库生效）。
2. **GitHub Pages 在国内可直连访问**（`*.github.io` 解析到 185.199.108.153 等 IP，未被墙）→ 手机看日报**不需要梯子**。注意：若梯子开着但规则未覆盖 github.io，反而会导致访问失败，直连更快。
3. GitHub Actions 提示 Node.js 20 弃用警告（actions/checkout@v4 等），暂不影响运行，后续可升级 action 版本。
4. Windows 控制台 GBK 编码无法打印 emoji，脚本内 print 一律用纯 ASCII 标记。

**验收对照（tasks.md）：**
- [x] 每天 08:10 / 15:40 各跑一次，可手动触发
- [x] 三个密钥全部走 GitHub Secrets
- [x] 日报部署到 gh-pages → Pages 开启 → 线上可访问
- [x] `.gitignore` 覆盖 docs/keys.md、data/、reports/、本地临时文件
- [x] 海外环境源可用性实测并记录（6/6 成功）

**下一步：** M8 集成测试与端到端验证（连续观察 3 天云端产出 + 用户试用反馈）

---

## 2026-09-16 · M6 微信推送模块 ✅ 完成

**功能：** 通过 Server酱（Turbo版 `sctapi.ftqq.com`）把日报摘要推送到微信。

**实测：** ✅ 成功推送（pushid=56628027），微信收到通知。标题 + 情绪一句话 + 3 条重点新闻 + 日报链接。

**推送内容结构：**
- `title`：`📊 股市情报日报 2026-09-16`（微信通知标题）
- `desp`：`### 今日市场`（M3 结论句）+ `### 重点新闻`（3 条标题）+ 日报链接

**重点新闻选择算法：**
- 排序：confirmed 优先 > 类别权重（policy 5 > stock 4 > industry 3 > international 2 > other 1）> 情绪显著性（利好/利空 > 中性）
- 标题抽取：优先【…】内文本，否则截断；统一上限 40 字

**实现要点：**
- SENDKEY 从环境变量 `SERVERCHAN_SENDKEY` 读取，未设置则优雅跳过（不抛异常）
- 推送失败只打印 warn 日志并继续打印内容，**不影响日报已生成的事实**
- 日报链接由 `REPORT_BASE_URL` 拼接（M7 起为 GitHub Pages，本地测试时未设置则提示暂不附链接）

**踩过的坑：**
1. 同一 confirmed 事件（油轮网络攻击）来自 2 个源，被 M2 标为 2 条，`select_top_news` 会重复选中 → 加标题主干去重（_stem 归一化取前 12 字）
2. 政策文件标题 50+ 字过长，微信推送可读性差 → extract_title 统一上限 40 字

**验收对照（tasks.md）：**
- [x] 用 Server酱，SENDKEY 走配置（环境变量）
- [x] 推送内容 = 情绪一句话 + 3 条重点新闻标题 + 日报链接
- [x] 推送失败仅记录日志，不影响日报生成
- [x] 实际收到一条测试推送（pushid=56628027）

**下一步：** M7 GitHub Actions 工作流 + Pages 部署

---

## 2026-09-16 · M5 网页日报生成模块 ✅ 完成

**功能：** 把 M1~M4 产物（raw_news / structured_news / analysis.md / quotes.json）整合成一份自包含 HTML 日报 + 索引页。

**输出：**
- `reports/2026-09-16.html`（83KB，单文件无外部依赖）
- `reports/index.html`（按日期倒序索引）

**版块结构（tasks.md 六项全满足）：**
1. 顶部摘要区（自动从 M3 结论句抽取，24 字，≤200 字达标）
2. 市场情绪概览（只取分析第一节，避免与完整分析重复）
3. 个股行情一览（按涨跌幅绝对值倒序，红涨绿跌，29 只 + 3 只失败标注）
4. 分板块新闻（按 policy/stock/industry/international/other 分组，带 已确认/待核实 徽章 + 情绪徽章 + 板块/个股标签 + 原文链接）
5. 完整分析（M3 markdown → HTML）
6. 固定风险声明页脚

**移动端适配：**
- 单栏 `max-width:760px`，`viewport` meta + `-webkit-text-size-adjust`
- 表格套 `overflow-x:auto` 容器，页面本身无横向滚动
- 暗色主题默认，`prefers-color-scheme` 自动切浅色，CSS 变量统一管理
- 系统字体栈（PingFang SC / Microsoft YaHei）保证中文渲染

**实现要点：**
- 最小 markdown 转换器（#/##/###、**加粗**、列表、表格、---），针对 M3 固定输出结构，实测 0 残留 markdown 符号
- 所有动态文本（新闻正文、来源、个股名）先 `html.escape` 再拼接，防注入
- 摘要抽取：正则抓"结论：..."行，去加粗标记；初始版只抓到加粗片段 18 字过薄，改为抓整行后 24 字

**踩过的坑：**
1. Windows 控制台 GBK 编码无法打印 emoji（✅）→ print 改用纯 ASCII 标记
2. 初版"情绪概览"与"完整分析"渲染了同一份全文（重复两遍）→ 拆分 extract_overview 只取第一节
3. markdown 表格表头判定依赖下一行 `---|---` 分隔，需先探测再定 th/td

**验收对照（tasks.md）：**
- [x] 手机优先设计（单栏/大字号/夜间友好）
- [x] 六版块齐全 + 确认/待核实标记 + 原文链接
- [x] 文件名按日期 `reports/2026-09-16.html`
- [x] 索引页按日期倒序
- [x] 无横向滚动（表格独立滚动容器）、暗色主题可读

**下一步：** M6 微信推送模块（Server酱 SendKey 已就绪）

---

## 2026-09-16 · M4 行情数据模块 ✅ 完成

**功能：** 从 M2 结构化新闻提取个股名称，通过东财搜索接口解析为 secid，再调东财行情接口拉取现价与涨跌幅。

**实测结果：** 32 只个股提取 → 29 只成功解析（A股 22 / 港股 1 / 美股 1），失败 3 只（渣打银行、LMAX集团、美的集团H股——均为搜索接口无匹配或 MktNum 未支持的市场）。

**实现要点：**
- `resolve_stock()`：东财搜索接口（searchadapter.eastmoney.com），MktNum 直接映射 secid 前缀（1=沪A / 0=深A / 116=港股），不再猜市场
- `fetch_quote()`：东财 push2 接口，带指数退避重试（5s/10s/15s），接口限流敏感故请求间隔 1s
- `extract_names_from_news()`：从 M2 `structured_news.json` 提取个股名，正则过滤噪音（纯代码 `02331.HK`、非中文名等）
- 输出：`data/quotes.json`（含 code、name、price、prev_close、change_pct、market）

**踩过的坑：**
1. 东财 push2 对突发请求限流（RemoteDisconnected）→ 增加指数退避 + 间隔 1s
2. 个股名提取噪音：`02331.HK` 类纯代码、含括号后缀 → 正则过滤
3. MktNum 映射：接口版本间不一致，最终用 MktNum 直接做 secid 前缀，最简洁

**验收：** ✅ 成功 29/32 只（90%+），覆盖 A 股与港股，3 只失败为预期边界（外资/小众市场）

**下一步：** M5 网页日报生成模块

---

## 2026-09-16 · M3 AI 深度分析模块 ✅ 完成

**流程：** 145 条结构化新闻 → 权重排序压缩成 122 条摘要（24K 字符）→ DeepSeek 一次性生成完整分析报告（5629 字符）

**Prompt 硬约束全部生效（实测验证）：**
- 【防编造】报告中 71 处新闻编号引用，经程序校验 **0 处越界**（所有引用都指向真实存在的输入新闻编号）；抽查 3 条论断（[105] 工程机械交流会、[74] 华电辽能储能增资、[98] 零跑定增）均与原文一致
- 【置信度】每个板块/个股判断均带（置信度:高/中/低）；单源(unve)新闻支撑的判断最高只给"中"——模型确实遵守了这个规则（大量"中"和"低"，无单源新闻获"高"）
- 【禁投资指令】程序化禁用词扫描：0 命中
- 风险提示 5 条，第 1 条按规范固定为"利好出尽"提示

**输出质量亮点（超出验收预期）：**
- 识别出"德尔未来股价异动与石墨烯业务基本面背离"（收入仅16.93万元 vs 2连板）
- 股权变动类新闻统一归并"不构成基本面判断"，避免散户常见的"外资增持=利好"误读
- 主动标注"备忘录为非约束性""核查期内有交易"等原文中的关键限定条件

**实现要点：**
- digest 构建：policy > stock > industry > international 权重排序，confirmed 优先，单条截断 200 字，总量 24K 字符
- postcheck 输出合规程序化扫描：投资指令禁用词 8 个 pattern + 置信度标注计数
- 用户已注册 Server酱（SendKey 待写入 docs/keys.md，M6 使用）

**输出文件：** `data/analysis.md`
**下一步：** M4 行情数据模块（东财行情接口已验证）

---
