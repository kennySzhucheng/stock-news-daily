# 数据源清单与测活状态

多元化原则：不止财经垂直媒体，覆盖综合门户、科技、国际、官方四类。准确性原则：多源交叉验证。

## 一、已验证可用（2026-09-16，本地网络）

| 来源 | 接口 | 类别 | 状态 |
|---|---|---|---|
| 新浪财经 7×24 | `zhibo.sina.com.cn/api/zhibo/feed?zhibo_id=152` | 财经快讯 | ✅ 已接入 M1（实测 399 条/天） |
| 东方财富快讯 | `np-weblist.eastmoney.com/...&req_trace=1`（showTime 为字符串时间） | 财经快讯 | ✅ 已接入 M1（实测 300 条/天） |
| 东方财富行情 | `push2.eastmoney.com/api/qt/stock/get`（secid: 沪`1.600519`/深`0.000001`） | 行情 | ✅ 实测返回正常（M4 接入） |
| 36氪 | `https://www.36kr.com/feed`（RSS，XML 有脏字符需清洗+正则回退） | 科技 | ✅ 已接入 M1（实测 30 条/天） |
| 新华网时政 | `http://www.xinhuanet.com/politics/news_politics.xml`（RSS） | 官方综合 | ✅ 已接入 M1（实测 300 条/天） |
| 人民网时政 | `http://www.people.com.cn/rss/politics.xml`（RSS） | 官方综合 | ✅ 已接入 M1（实测 100 条/天） |
| 中国政府网政策库 | `sousuo.www.gov.cn/search-gov/data?t=zhengcelibrary_gw`（JSON，listVO 键） | 政府发布 | ✅ 已接入 M1（实测 40 条/天） |

> M1 实测单日合计去重后 **1115 条**，四类分布：财经 655 / 科技 30 / 政策 40 / 官方综合 390。

## 二、GitHub Actions 海外环境实测 ✅（2026-09-16，M7 首次云端运行）

> 对应 tasks.md M7 第 5 条：Actions 服务器在海外，财经源可能有地域差异，需实测并记录。

| 来源 | 本地返回 | 云端返回 | 结论 |
|---|---|---|---|
| 新浪财经 7×24 | 399 / 300 条 | 300 条 | ✅ 可用（条数随时间窗口波动，非地域差异） |
| 东方财富 7×24 | 300 条 | 300 条 | ✅ 可用（与本地一致） |
| 36氪 | 30 条 | 30 条 | ✅ 可用（一致） |
| 新华网 | 300 条 | 300 条 | ✅ 可用（一致） |
| 人民网 | 100 条 | 100 条 | ✅ 可用（一致） |
| 中国政府网 | 40 条 | 40 条 | ✅ 可用（一致） |

**结论：6 个源全部可用，0 失败。** 海外服务器抓取国内源无地域障碍，**不需要国内中转**。

新浪 7×24 的返回条数存在波动（本地首次 399 条，此后本地与云端均为 300 条），
经复测确认**与地域/IP 无关**，而是该接口自身的时间窗口差异；300 条远超单日分析所需。

**行情接口补充（2026-09-16 M8 实测）：** 东财 `push2` 存在按 IP 的临时限流
（表现为 `RemoteDisconnected`），限流期间重试无效。M4 已加入备选源：

| 行情源 | 接口 | 格式 | 实测 |
|---|---|---|---|
| 东方财富（主） | `push2.eastmoney.com/api/qt/stock/get` | JSON | ✅ 正常时限流敏感 |
| 腾讯（备选） | `qt.gtimg.cn/q=sh600519` | 文本，GBK，`~` 分隔 | ✅ 0.3s 稳定，无需 Referer |
| 新浪（二级备选） | `hq.sinajs.cn/list=sh600519` | 文本，GBK，需 Referer | ✅ 0.2s |

> 行情接口（东财 `push2`）云端同样可访问——M4 步骤在本次运行中成功完成。
>
> 本次云端运行完整耗时约 12 分钟（M1→M5），其中 M2 的 GLM 批量调用占大头（约 10 分钟），距离 30 分钟的 workflow 超时上限仍有充足余量。

## 三、后续候选（按需补充）

| 来源 | 接入方式 | 类别 | 用途 |
|---|---|---|---|
| 路透中文 / 参考消息 | 待验证 RSS | 国际 | 外围市场联动（feedburner/dj 等海外源在国内环境不稳定，暂缓） |
| 发改委 / 央行 / 证监会官网 | 官网站点解析 | 政府发布 | 已有 gov.cn 政策库覆盖国务院层级，部委可按需加 |
| 新浪 7×24 硬性的每人每日频率需遵守 UA/间隔约束，勿加密至分钟级 | — | — | — |

## 四、放弃的源

| 来源 | 原因 |
|---|---|
| 财联社电报 | 接口有反爬（2026-09-16 实测返回 HTML 而非 JSON）。**2026-09-25 复核仍不可用**：`cn-financial-scraper` skill 的 `cls_scraper` 打 `https://www.cls.cn/api/sw` 返回 **404**（端点已变更），故仍然放弃 |
| 新浪滚动 `lid=2510`（要闻） | 2026-09-25 实测返回的是 **2~7 个月前的旧闻**（02-02 / 05-26 / 08-29），滚动机制已失效 |
| `query_financial_data("news")` 统一入口 | 东财适配器缺 `client` 参数，上游返回 `Required String parameter 'client' is not present`，而适配器把该报错当成功返回——**错误未被拦住** |

## 五、交叉验证规则

- 同一事件（AI 判断或标题相似度）被 **≥2 个独立来源**报道 → `已确认`
- 仅单源 → `单源待核实`，在日报中永久保留该标记
- 政策类新闻：若与政府官网原文一致 → 可升级为最高置信
- 注意"转载不算独立源"：转载与原发需判别，只有独立采访/独立发布才算第二源

## 六、维护约定

M1 实施时：每接入一个新源先在本文件登记接口格式与验证结果；上线后每周（M7 工作流可自动附带）测活一次，失效源标记并在 CHANGELOG 记录更换方案。

## 七、第二阶段新增（2026-09-25，三个源）

端点取自新装的 `cn-financial-scraper` skill，但**没有直接 import 它**：skill 装在
`~/.claude/skills/`、**不是 git 仓库**，云端 runner 拿不到；且它依赖 `requests` +
自带的 `http_utils`（1932 行）。故只**取端点**，按本模块的纯标准库风格重写
（复用 `_urlopen` / `_post_form` / 0.6s 间隔 / UA），workflow 无需加 `pip install`。

| 来源 | 接口 | 类别 | 实测 |
|---|---|---|---|
| 巨潮资讯公告 | `POST www.cninfo.com.cn/new/hisAnnouncement/query`（表单） | **个股公告** | ✅ 33 条/次，9 源全绿 |
| 东方财富宏观政策 | `np-listapi.eastmoney.com/comm/web/getNewsByColumns?column=345` | 财经/政策解读 | ✅ 40 条 |
| 新浪财经滚动 | `feed.mix.sina.com.cn/api/roll/get?lid=2516` | 国际/港股视角 | ✅ 25 条 |

### 巨潮：两个静默的坑（都已在代码里设防）

1. **类目码无效不报错，而是静默回退成「全部公告」**（约 1400 条/天）。
   2026-09-25 实测：`gqbd`（股权变动）/ `rcjy`（日常经营）/ `yjygjxz`（业绩预告）/
   `sjdbg` / `yjdbg` **有效**；skill 里带中文标签的 `zcjy` / `gdqz` / `gdzc` / `hg` /
   `ndbg` / `bndbg` **对线上 API 无效**（返回全部 = 未过滤）。
   → `fetch_cninfo()` 先取「全部」基线，逐类目比对 `totalRecordNum`，
   **对不上就丢弃该类目并打印告警**，宁可少抓也不能抓错。
2. **`announcementTime` 只有日期，时刻恒为北京 00:00**，`column` 参数实际被忽略
   （`szse` 与 `sse` 返回完全相同结果，靠 `_szsh` 后缀即覆盖沪深）。
   → 时间戳如实照抄、不编造时刻；代价是这些条目在「按时间倒序」里排到当日快讯之后，
   故 category 提示用 `announcement` 并由 M2 的 `PRIORITY_CATEGORIES` 优先保留。

### 与 M2 预筛的耦合（改动 M2 的原因）

`prefilter_local` 原来只有 `policy` 优先。新增公告后实测：**32 条公告 0 条存活** ——
它们的时间戳全是 00:00，而 150 条截断线当天在 11:02，整批被挤掉。
现改为 `PRIORITY_CATEGORIES = ("policy", "announcement")`，实测 **32/32 存活**。

预筛分布变化（同一份 raw_news，1098 条 → 150 条）：

| | 公告 | 政策 | 财经 | 科技 |
|---|---|---|---|---|
| 改前 | 0 | 40 | 105 | 5 |
| 改后 | **32** | 40 | 75 | 3 |

代价是财经快讯少进 30 条、科技少 2 条 —— 用通用快讯的名额换成了逐条可定位到
个股的公告，净值为正。各源存活率：巨潮 100%、新浪滚动 84%、中国政府网 100%、
东方财富宏观 8%、快讯类 5~13%（快讯本就重复度高）。

> **待验收**：M2 的 GLM 端到端本地**未跑**——`ZAI_API_KEY` 不在 `docs/keys.md`
> （该行只写了「用户现有智谱密钥」）。`prefilter_local` / `normalize_cat` 是纯本地
> 函数、已离线验过；GLM 调用本身未改动。补验方式见 CHANGELOG 当天条目。
