# 模块任务说明

每个模块有独立的验收标准，做完勾选并更新 CHANGELOG.md。

---

## M0 项目骨架与文档 ✅

- [x] 目录结构建立
- [x] README.md 总览
- [x] docs/ 四份基础文档
- [x] CHANGELOG.md 日志机制

---

## M1 新闻收集模块

**职责：** 从多个来源抓取最近 24h 新闻原文，输出统一 JSON 格式。

**要求：**
1. 多元化来源（见 sources.md），每类至少一个源
2. 每条新闻统一结构：`{time, source, url, category_hint, text}`
3. 跨源去重（同一事件保留最早/最权威一条）
4. 失败的来源跳过并在输出中标记，不阻塞整体
5. 请求间隔 ≥0.5s，带 User-Agent，不做高频抓取

**输出文件：** `data/raw_news.json`

**验收：** 本地运行能产出 ≥50 条/天的新闻，含至少 3 个不同类别来源

---

## M2 新闻筛选与结构化模块

**职责：** 用免费模型（GLM-4-Flash）把原始新闻变成结构化清单。

**要求：**
1. 分类：宏观政策 / 行业动态 / 个股公告 / 国际市场 / 其他
2. 对每条提取：涉及板块、涉及个股（名称+可辨识代码）、情绪倾向（利好/利空/中性）
3. **交叉验证打标**：与其他源报道同一事件的 → `已确认`，否则 → `单源待核实`
4. 丢弃与股市无关的纯社会新闻（但保留宏观政策类即使看似间接）
5. 单源待核实的条目在后续所有展示中必须保留该标记

**输出文件：** `data/structured_news.json`

**验收：** 对一天数据跑完后，分类准确率人工抽查 ≥80%，确认/待核实标记正确

---

## M3 AI 深度分析模块

**职责：** 用 DeepSeek 对结构化新闻做综合分析。

**要求（prompt 硬约束）：**
1. 只允许基于输入的新闻原文分析，禁止编造未提供的消息 → 在 prompt 中明确"你不知道的不要写"
2. 输出结构固定：
   - 今日市场情绪概览（一段话 + 乐观/中性与谨慎的判断）
   - 值得关注的板块（2-4 个，说明逻辑链：哪条新闻 → 为什么可能与股价相关）
   - 涉及个股清单（仅列出新闻中出现的，附"新闻关联点"与"不确定性说明"）
   - 风险提示（至少 2 条，含"新闻≠股价"的常规提醒）
3. 所有判断句附置信度（高/中/低）
4. 输出中不得出现"建议买入/卖出"字样，只允许"值得观察""建议持续跟踪"等中性表述

**输出文件：** `data/analysis.md`

**验收：** 人工检查输出无编造内容（随机抽 3 条论断能在原始新闻中找到出处）

---

## M4 行情数据模块

**职责：** 抓取 M2 提取出的个股的当日行情，作为"新闻 vs 价格位置"的佐证。

**要求：**
1. 东方财富公开接口（已验证：`push2.eastmoney.com/api/qt/stock/get`）
2. 输入个股票代码列表（需自动推断沪深市场前缀：600→1.，000→0.，300→0.，003→0. 等）
3. 输出：名称、现价、今日涨跌幅、可加逻辑：5日涨跌幅
4. 接口失败静默跳过该个股

**输出文件：** `data/quotes.json`

**验收：** 任选 5 只含沪、深、创业板的股票代码，行情数据正确（与行情软件对照）

---

## M5 网页日报生成模块 ✅

- [x] 手机优先设计（单栏、大字号、夜间友好）
- [x] 版块① 推送摘要区（≤200字）
- [x] **版块② 市场分析（M3 全文）—— 置于新闻列表之前，先给判断再给素材**
- [x] 版块③ 个股行情一览
- [x] 版块④ 分板块新闻列表（带确认/待核实标记与原文链接）
- [x] 版块⑤ 固定风险声明页脚
- [x] 顶部版块跳转导航
- [x] **文件名按日期 + 时段 `reports/YYYY-MM-DD-am.html` / `-pm.html`（盘前盘后互不覆盖）**
- [x] `reports/latest.html` 固定入口，始终指向最近一次运行
- [x] 索引页按日期分组，每天列出盘前/盘后两份
- [x] 手机浏览器无横向滚动、暗色主题可读

**版块顺序说明：** 2026-09-16 按用户反馈调整——原「完整分析」在页面最末，
现提到「个股行情」与「分板块新闻」之前，并与摘要区下方原「情绪概览」合并
（两者本是同一份 M3 文本，此前重复渲染了两遍）。

---

## M6 微信推送模块 ✅

- [x] 用 Server酱（sct.ftqq.com），SENDKEY 走环境变量配置
- [x] **推送内容顺序：市场分析（结论 + 关注板块逻辑链）→ 3 条重点新闻 → 日报链接**
- [x] 推送失败仅记录日志，不影响日报已生成
- [x] 实际收到一条测试推送（pushid=56628027）
- [x] 标题带时段（盘前/盘后），链接指向对应的 `YYYY-MM-DD-am|pm.html`

**调整说明：** 2026-09-16 按用户反馈——原来正文只有一句情绪结论，
现改为先给完整的市场分析（情绪结论 + 最多 3 个关注板块的逻辑链），
新闻标题退居其后，让人不点开链接也能先拿到判断。

---

## M7 GitHub Actions 工作流 + Pages 部署 ✅

- [x] 工作流每天 08:10 与 15:40（北京时间）各跑一次，可手动触发
- [x] 密钥全部走 GitHub Secrets（ZAI_API_KEY / DEEPSEEK_API_KEY / SERVERCHAN_SENDKEY）
- [x] 日报部署到 gh-pages 分支 → GitHub Pages 开启 → 线上可访问
- [x] `.gitignore`：docs/keys.md、data/、reports/、本地临时文件
- [x] 海外环境源可用性实测并记录（**6/6 全部可用，0 失败**，见 sources.md 第二节）

**验收：** ✅ 云端跑出第一份线上日报，直连可访问（无需梯子）
- 仓库：https://github.com/kennySzhucheng/stock-news-daily
- 线上：https://kennyszhucheng.github.io/stock-news-daily/
- 首次运行 run 35092383171：全部步骤成功，耗时约 12 分钟

---

## M8 集成测试与端到端验证（进行中）

- [x] 本地端到端跑一次，产出完整日报（2026-09-16 · 全链路约 6.8 分钟，各环节产出校验全部通过）
- [x] 云端手动触发验证一次：run 35112231681，18 个步骤全成功，8 分 26 秒；
      微信推送成功（pushid=56655308），网页版已部署到 `/web/`
- [x] **cron 定时任务激活验证**：发现 cron 从未触发过（所有历史运行均为手动触发），
      2026-09-17 手动触发 workflow（run 35169710420）激活 cron 调度，M6 推送成功（pushid=56723358）
- [ ] 连续观察 3 天云端**定时**运行（9/17–9/19），核对是否如期触发
      （用 `python modules/m8_e2e/observe.py`）
- [ ] 用户试用反馈：日报长度、信息密度是否合适，据此微调
- [x] **定时延迟核对（9-18）**：cron 确实会跑，但延迟 4~5 小时；已加延迟标注与
      外部触发（`tools/trigger_workflow.py`），详见 CHANGELOG 9-18 条目
- [x] **去重改为按时段判定（9-18）**：旧逻辑按「最近 N 小时是否跑过」判断，不分时段——
      9-17 盘前延迟到 12:46 跑完后，15:40 的盘后外部触发被误判为重复而 skip，
      盘后只能退回等延迟的 cron（20:53 才推）。现改为查「今天的这个时段是否已出报」
- [x] **云端端到端实测（9-18）**：外部触发 HTTP 204 → 全链路 8 分 26 秒跑通并部署；
      随后延迟的盘前 cron 触发时被去重挡住（M1→Pages 全 skipped，无重复推送）；
      延迟计算在 runner 上实测 `计划时点 08:10 / 延迟 272 分钟`，与执行时刻吻合
      （确认 `TZ=Asia/Shanghai` 在 ubuntu runner 上生效，不需要改算法）
- [ ] 延迟**标注渲染**的云端验证：本次被延迟的 cron 因去重跳过了 M5，未走到渲染；
      改为等一次「没有先手触发」的延迟运行，或本地已用真实数据验证渲染无误
- [ ] 配外部调度器按点触发：**cron-job.org** POST `workflow_dispatch`
      （08:10 盘前 / 15:40 盘后，Asia/Shanghai）。需 fine-grained PAT，
      **不要用 `gh auth token`**（其 `repo` scope 覆盖全部仓库，交给第三方风险过大）
- [ ] 观察一次真正的按点触发，用 `observe.py` 核对实际开始时间与计划时点的偏差

**过程中修复的缺陷（详见 CHANGELOG M8 条目）：**
- Windows 系统代理导致全链路网络请求失效（各模块已加 `_urlopen` 优先直连）
- M4 行情接口限流拖垮流水线（已加源级熔断 + 腾讯备选源）

**验收：** 用户确认上一期日报"看得懂、有用"

---

## M9 交互式网页版（本地服务 + 静态导出）✅

**职责：** 补齐推送版做不到的能力——检索、聚合、追问。
推送版是"读一遍就走"的长文，M9 面向"我想自己找"的场景。

**要求：**
1. 同一套前端跑两种数据源：本地服务（`/api/*`）与静态导出（`./api/*.json`）
2. 只依赖 Python 标准库，不需要 pip 安装任何东西
3. 数据更新后刷新页面即可，服务不重启也能拿到新数据
4. 分析正文里的引用编号 `[12]` 可点击跳转到出处新闻

**功能（均多于推送版）：**
- [x] 总览：情绪仪表（含基数说明）、统计卡、多空分布、分类分布、数据源状态、涨跌榜、热门板块
- [x] 新闻：全文搜索（空格分隔为 AND）+ 分类/情绪/来源数/来源/板块/个股 六维筛选 + 三种排序 + 分页
- [x] 原始新闻视图：可查看被 M2 过滤掉的 1000+ 条，并标明是否被选中
- [x] 详情弹层：全文 + 元信息 + 相关行情 + 同板块同个股的关联新闻（可继续跳转）
- [x] 行情：可点表头排序的表格，含未取到行情的个股说明
- [x] 板块：按新闻数排序的板块卡，带多空分布条与关联个股
- [x] 深度分析：M3 全文（引用可点击）+ **AI 追问**
- [x] 历史：按日期列出盘前/盘后，可直接打开对应日报
- [x] 静态导出随日报一起部署到 Pages（`/web/`）
- [x] `sync.py`：把云端每天的最新产出拉到本地，本地不必重跑流水线
      也能看到当天内容；`server.py --source auto|local|export` 选择数据来源

**AI 追问的硬约束（与 M3 一致）：**
1. 只基于给定新闻作答，每个论断标注出处编号 `[n]`
2. 每条判断附置信度；单源待核实新闻支撑的判断最高只给"中"
3. 禁止投资指令
4. **新闻不足以回答时必须明说**，并指出还缺哪类信息，不得用模型自身知识补位
5. 检索按关键词打分挑相关新闻，用户勾选的新闻必进上下文

**验收：**
- [x] `python modules/m9_web/check.py` 自检 17 项全通过
- [x] 无头浏览器渲染验证：总览/新闻/板块三屏均正确渲染，无 JS 错误
- [x] AI 追问实测：问"半导体设备相关消息"，正确检索到 [71][94][97] 三条，回答区分了"新闻里说了什么"与"据此能推到/推不到什么"
- [x] 静态导出在浏览器中可用（除追问外功能齐全）

---
